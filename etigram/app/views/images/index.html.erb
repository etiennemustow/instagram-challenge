<head>
  <link rel="stylesheet" href="images.scss">
</head>
<p id="notice"><%= notice %></p>

<h1 style="text-align: center">Feed</h1>

<style>
table {
 margin-left: auto;
 margin-right: auto;
 width: 100px;
}

td{
  width: 1px;
}
</style>

<table colspan="6">
    <% @images.each do |image| %>

      <tr>
        <td><%= image_tag image.image.correct.url %></div></td>
      </tr>
      <tr>
        <td style="text-align: left"> <%= image.description %></td>
      </tr>
      <tr>
        <% image.comments.each do |c|%>
        <td><%= c.commenter %><td>
      </tr>
      <%end%>
    <tr>
      <td><%= link_to 'View', image %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Image', new_image_path %>
